{% extends "base.html.j2" %}

{%block title%}ABC{%endblock%}

{% block obsah %}

Tady jsem si to přepsal.


<h1>Podíl</h1>

<form  method="get">
   <input type="number" name='x' value="{{ request.args.get('x') or ''}}"> /
   <input type="number" name='y' value="{{ request.args.get('y') or ''}}"> = {{podil}} 

    <button name="submit">Podělit</button>
</form>

<form method = "get">
    <input typě="text" name = "slovo">
    <button type="submit">Odeslat</button>

{% for i in  range(1,7) -%}
<h{{i}} >Nadpis {{ i }}</h{{i}}>
{% endfor -%}


{% endblock obsah %}

